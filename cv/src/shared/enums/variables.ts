export enum Colors {
  BASIC = '--basic-color',
  GRAY = '--light-gray',
  WHITE = '--white',
  BLACK = '--black',
  ORANGE = '--orange',
  BLUE = '--blue',
}

export enum SessionStorageKeys {
  CHIPS_INDEX = 'sessionStorage',
  LANG = 'lang',
}
