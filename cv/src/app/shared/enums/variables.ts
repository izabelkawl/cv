export enum Colors {
  BASIC = '--basic-color',
  WHITE = '--white',
  BLACK = '--black',
  TEXT_COLOR = '--text',
}

export enum SessionStorageKeys {
  CHIPS_INDEX = 'sessionStorage',
  LANG = 'lang',
}
